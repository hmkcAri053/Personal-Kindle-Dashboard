
  # Dashboard (Make)

  This is a code bundle for Dashboard (Make). The original project is available at <YOUR_FIGMA_URL>.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  